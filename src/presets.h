#pragma once
#include "squares.h"
#include "gamestate.h"
#include "stdbool.h"

// Preset names
void everyThingOwnedByPlayer0(gamestate *game);

void midGame(gamestate *game);

void endGame(gamestate *game);